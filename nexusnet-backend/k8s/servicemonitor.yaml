apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nexusnet-api
  namespace: nexusnet
spec:
  selector:
    matchLabels:
      app: nexusnet-api
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
