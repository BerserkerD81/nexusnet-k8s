apiVersion: v1
kind: ConfigMap
metadata:
  name: nexusnet-config
  namespace: nexusnet
data:
  NODE_ENV: production
  DB_HOST: postgres
  REDIS_HOST: redis
  LOG_LEVEL: info
  CORS_ORIGIN: https://nexusnet.example.com
